namespace AdventOfBlazor.BlazorClient
{
    public class Settings
    {
        /// <summary>
        /// Holds the path prefix used to access the app.  By default is / in dev get's updated by deployment pipeline to /advent-of-blazor/
        /// </summary>
        public const string WEB_PATH_PREFIX = "/";
    }
}
